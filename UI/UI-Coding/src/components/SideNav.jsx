import React from "react";
import "./SideNav.css";

const SideNav = () => (
  <nav className="side-nav">
    <div className="side-nav__line"></div>
    <div className="side-nav__label">Home</div>
  </nav>
);

export default SideNav;
